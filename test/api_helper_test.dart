import 'package:mockito/annotations.dart';
import 'package:dashboard/helpers/network/api_helper.dart';

// GenerateMocks akan menghasilkan file mock untuk ApiHelper
@GenerateMocks([ApiHelper])
void main() {
  // Kosongkan, kita hanya menggunakan ini untuk generate mock.
}
